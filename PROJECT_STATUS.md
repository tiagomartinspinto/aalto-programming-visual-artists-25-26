# Project Status

## Completed This Pass

- Standardized the 2024-2025 and 2025-2026 year pages as one interface system instead of two separate page designs.
- Added `assets/year-common.css` as the shared source for year-page layout, card rhythm, section spacing, controls, slide reader behavior, responsive behavior, and PDF fallback styling.
- Reduced each local `year.css` to intentional year-specific tokens only: route label and palette values.
- Updated both year pages to load the shared year interface stylesheet before the local palette stylesheet.
- Matched the top nav, course header, current session card, course search, web sketches, Sketch Lab, Processing vs p5.js, Slides Reader, Projects, Sessions, footer, and mobile behavior through shared CSS.
- Kept year differences limited to year label, counts, titles/descriptions/content, links, and intentional palette tokens.
- Kept all visible content, slide links, session links, sketch links, source links, and project material.
- Redesigned the homepage authorship/responsibility note as a quiet footer-level `site-note` instead of a warning-style block.
- Kept the no-iframe PDF approach and safe PDF links with `target="_blank" rel="noopener noreferrer"`.
- Kept the current removed-coursework-trace checks and did not reintroduce private coursework references.
- Updated the future-year scaffold so new years start with the shared interface stylesheet.
- Tightened tests so future year pages fail if one year silently drifts away from the shared structure.

## Files Changed

- `PROJECT_STATUS.md`
- `assets/ascii-skin.css`
- `assets/home.css`
- `assets/year-common.css`
- `index.html`
- `tools/check-site.mjs`
- `tools/new-year.mjs`
- `tools/smoke-test.mjs`
- `years/2024-2025/index.html`
- `years/2024-2025/year.css`
- `years/2025-2026/index.html`
- `years/2025-2026/year.css`

## Interface Standardization Details

- Both year pages now use the same shared layout CSS for:
  - top navigation
  - course header
  - current session and course search cards
  - web sketch cards and lazy preview frames
  - Sketch Lab feature cards
  - Processing vs p5.js comparison cards
  - Slides Reader controls and PDF fallback panel
  - Projects and Sessions grids
  - footer
  - desktop, tablet, and mobile breakpoints
- Both Slides Readers now use the same source HTML shell:
  - `.slides-reader`
  - `.slide-controls`
  - `.slide-fallback-list`
  - `.slide-viewer`
  - `.slide-viewer-bar`
  - `.slide-actions`
  - `#prev-slide-deck`
  - `#next-slide-deck`
  - `#slide-direct-link`
  - `.pdf-panel#slide-panel`
  - `#slide-panel-message`
- Both enhanced Slides Readers now use the same select-first enhancement pattern.
- Shortcut buttons are generated by the same logic for both years; 2024-2025 enters compact mode because it has 8 decks, while 2025-2026 keeps shortcuts visible because it has 6 decks.

## Tests Run

- Syntax checks with bundled Node:
  - `node --check tools/check-site.mjs`
  - `node --check tools/smoke-test.mjs`
  - `node --check tools/new-year.mjs`
- Static and asset checks:
  - `node tools/build-course-index.mjs`
  - `node tools/check-site.mjs` - passed
  - `node tools/check-assets.mjs` - passed
  - `git diff --check` - passed
- Browser smoke test against `http://127.0.0.1:8123/` - passed:
  - desktop 1280px
  - tablet 820px
  - mobile 390px
  - JavaScript enabled
  - JavaScript disabled
  - slide select behavior
  - previous/next deck behavior, including wraparound
  - direct PDF link safety
  - no embedded year-level PDF iframe
  - no horizontal overflow
  - session PDF panels
  - sketch preview sandboxing
  - Lab edit/run flow

## Visual Comparison Results

- Desktop 1280px:
  - `2024-2025`: 8 enhanced slide options, compact slide controls, 300px reader control column, no horizontal overflow.
  - `2025-2026`: 6 enhanced slide options, shortcut buttons visible, 300px reader control column, no horizontal overflow.
- Tablet 820px:
  - Both pages collapse Slides Reader to a single-column `780px` layout with matching nav wrapping and no horizontal overflow.
- Mobile 390px:
  - Both pages use the same single-column slide reader behavior, visible select control, matching stacked nav rhythm, and no horizontal overflow.
- JavaScript disabled:
  - `2024-2025`: 8 visible fallback PDF links.
  - `2025-2026`: 6 visible fallback PDF links.
  - Both use the same fallback list structure and safe new-tab PDF attributes.
- Homepage authorship note:
  - Rechecked in browser as a subtle `aside.site-note` with only a quiet top rule, 13px text, and no warning-box treatment.

## Remaining Issues

- No local interface drift remains between the two year pages in the tested structure.
- After push, the live GitHub Pages deployment should be checked once Actions finishes.
- The exact final commit hash is recorded in the final handoff response because a Git commit cannot contain its own resulting hash inside `PROJECT_STATUS.md` without changing that hash.
