void setup() {
  size(400, 400);
  background(255);

  float r = randomRadius();

  fill(100, 150, 255);
  ellipse(200, 200, r * 2, r * 2);
}

// Function that returns a value
float randomRadius() {
  return random(30, 100);
}