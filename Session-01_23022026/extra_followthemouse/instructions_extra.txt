EXTRA EXERCISE – Mouse Drawing

This sketch draws a circle that follows the mouse.

Try the following:

Remove background(220).
What happens?

Change the ellipse size.

Make the size depend on mouseX or mouseY.

Change the color based on mouse position:
fill(mouseX, mouseY, 200);

Add randomness:
Use random() to vary size or color.

Make the circle leave a trail only when the mouse is pressed.
Hint: use mousePressed inside an if statement.